/*eslint-env node*/
const mySolution = require('../template.js');

describe('Problem #: Problem title', () => {
  it('mySsolution should be a function', () => {
    expect(typeof mySolution).toBe('function');
  });
});
